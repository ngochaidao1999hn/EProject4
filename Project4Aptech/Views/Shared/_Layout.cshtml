@using Project4Aptech.Models
@{
    ViewBag.Title = "Home Page";
    Account logged = new Account();
    string send = "/Home/Login";
    string his = "/Home/Login";
    string withdraw = "/Home/Login";
    if (Session["logged"] != null)
    {
        logged = (Account)Session["logged"];
        send = "/Accounts/ChuyenTien/"+logged.Customers.Id;
        his = "~/TransactHis/Index/"+logged.Customers.Id;
        withdraw = "/WithDrawing/Index";
    }
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="">
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- Site Icons -->
    <link rel="shortcut icon" href="#" type="image/x-icon" />
    <link rel="apple-touch-icon" href="#" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/bootstrap.min.css")
    @Styles.Render("~/Content/custom.css")
    @Styles.Render("~/Content/pogo-slider.min.css")
    @Styles.Render("~/Content/responsive.css")
    @Styles.Render("~/Content/style.css")
    @Scripts.Render("~/bundles/modernizr")

<style>
    .search-btn {
        background: none;
    }

</style>
</head>
<body id="home" data-spy="scroll" data-target="#navbar-wd" data-offset="98">

    <!-- LOADER -->
    <div id="preloader">
        <div class="loader">
            <img src="~/Images/loader.gif" alt="#" />
        </div>
    </div>
    <!-- end loader -->
    <!-- END LOADER -->
    <!-- Start header -->
    <header class="top-header">
        <div class="header_top">
            <div class="container">
                <div class="row">
                    <div class="logo_section">
                        <a class="navbar-brand" href="/Home/"><img src="~/Images//logo.png" alt="image"></a>
                    </div>
                    <div class="site_information">
                        @if (Session["logged"] != null)
                        {
                            <ul class="nav nav-pills">
                                <li><a href="mailto:ngochai.dao1999@gmail.com"><i class="fa fa-envelope"></i>@logged.Customers.email</a></li>
                                <li><a href="tel:ngochai.dao1999@gmail.com"><i class="fa fa-phone"></i>@logged.Customers.PhoneNumber</a></li>
                                <!-- Đưa đường dẫn logout vào đây -->
                                <li><a class="join_bt" href="/Home/Signout">Logout</a></li>
                            </ul>
                        }
                        else
                        {
                            <ul>
                                <li><a class="join_bt" href="/Home/Login">Login</a></li>
                            </ul>
                        }

                    </div>
                </div>
            </div>

        </div>
        <div class="header_bottom">
            <div class="container">
                <div class="col-sm-12">
                    <div class="menu_orange_section" style="background: #ff880e;">
                        <nav class="navbar header-nav navbar-expand-lg">
                            <div class="menu_section">
                                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-wd" aria-controls="navbar-wd" aria-expanded="false" aria-label="Toggle navigation">
                                    <span></span>
                                    <span></span>
                                    <span></span>
                                </button>

                                <div class="collapse navbar-collapse justify-content-end" id="navbar-wd">
                                    <ul class="navbar-nav">
                                        <li><a class="nav-link active" href="/Home/">Home</a></li>
                                        <li><a class="nav-link" href="/Home/About">About</a></li>
                                        <li><a class="nav-link" href="/Home/Contact">Contact</a></li>
                                        <li><a class="nav-link" href="@send">Send Money</a></li>
                                        <li><a class="nav-link" href="@withdraw">Withdrawal</a></li>
                                    </ul>
                                </div>
                            </div>
                        </nav>
                        <div class="search-box">
                            <input type="text" class="search-txt" placeholder="Search">
                            <a class="search-btn">
                                <img src="~/Images/search_icon.png" alt="#" />
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </header>
    <!-- End header -->
    <!-- Start Banner -->


    @RenderBody()


    <footer class="footer-box">
        <div class="container">
            <div class="row">
                <div class="col-md-12 white_fonts">
                    <div class="row">
                        <div class="col-sm-7 col-md-7 col-lg-4">
                            <div class="logo_section">
                                <a class="navbar-brand" href="index.html"><img src="~/Images//logo.png" alt="image"></a>
                            </div>
                        </div>

                        <div class="col-sm-6 col-md-6 col-lg-3">
                            <div class="full">
                                <div class="footer_blog full white_fonts">
                                    <h3>Register</h3>
                                    <p>Our policy changes information, promotions, customer support services</p>
                                    <div class="newsletter_form">
                                        <form action="index.html">
                                            <input type="email" placeholder="Your Email" name="#" required="">
                                            <button>Submit</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-6 col-md-6 col-lg-3">
                            <div class="full">
                                <div class="footer_blog full white_fonts">
                                    <h3>Contact us</h3>
                                    <ul class="full">
                                        <li><img src="~/Images//i5.png"><span>54 - 56 Lê Thanh Nghị, Bách Khoa,<br> Q. Hai Bà Trưng, Hà Nội</span></li>
                                        <li><img src="~/Images//i6.png"><span>ngochaidao1999@gmail.com</span></li>
                                        <li><img src="~/Images//i7.png"><span>1800 1147</span></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </footer>



    @Scripts.Render("~/Scripts/jquery.min.js")
    @Scripts.Render("~/Scripts/popper.min.js")
    @Scripts.Render("~/Scripts/bootstrap.min.js")
    <!-- ALL PLUGINS -->
    @Scripts.Render("~/Scripts/jquery.magnific-popup.min.js")
    @Scripts.Render("~/Scripts/jquery.pogo-slider.min.js")
    @Scripts.Render("~/Scripts/slider-index.js")
    @Scripts.Render("~/Scripts/smoothscroll.js")
    @Scripts.Render("~/Scripts/form-validator.min.js")
    @Scripts.Render("~/Scripts/contact-form-script.js")
    @Scripts.Render("~/Scripts/isotope.min.js")
    @Scripts.Render("~/Scripts/images-loded.min.js")
    @Scripts.Render("~/Scripts/custom.js")
    @RenderSection("scripts", required: false)

    <script>
        /* counter js */

        (function ($) {
            $.fn.countTo = function (options) {
                options = options || {};

                return $(this).each(function () {
                    // set options for current element
                    var settings = $.extend({}, $.fn.countTo.defaults, {
                        from: $(this).data('from'),
                        to: $(this).data('to'),
                        speed: $(this).data('speed'),
                        refreshInterval: $(this).data('refresh-interval'),
                        decimals: $(this).data('decimals')
                    }, options);

                    // how many times to update the value, and how much to increment the value on each update
                    var loops = Math.ceil(settings.speed / settings.refreshInterval),
                        increment = (settings.to - settings.from) / loops;

                    // references & variables that will change with each update
                    var self = this,
                        $self = $(this),
                        loopCount = 0,
                        value = settings.from,
                        data = $self.data('countTo') || {};

                    $self.data('countTo', data);

                    // if an existing interval can be found, clear it first
                    if (data.interval) {
                        clearInterval(data.interval);
                    }
                    data.interval = setInterval(updateTimer, settings.refreshInterval);

                    // initialize the element with the starting value
                    render(value);

                    function updateTimer() {
                        value += increment;
                        loopCount++;

                        render(value);

                        if (typeof (settings.onUpdate) == 'function') {
                            settings.onUpdate.call(self, value);
                        }

                        if (loopCount >= loops) {
                            // remove the interval
                            $self.removeData('countTo');
                            clearInterval(data.interval);
                            value = settings.to;

                            if (typeof (settings.onComplete) == 'function') {
                                settings.onComplete.call(self, value);
                            }
                        }
                    }

                    function render(value) {
                        var formattedValue = settings.formatter.call(self, value, settings);
                        $self.html(formattedValue);
                    }
                });
            };

            $.fn.countTo.defaults = {
                from: 0,               // the number the element should start at
                to: 0,                 // the number the element should end at
                speed: 1000,           // how long it should take to count between the target numbers
                refreshInterval: 100,  // how often the element should be updated
                decimals: 0,           // the number of decimal places to show
                formatter: formatter,  // handler for formatting the value before rendering
                onUpdate: null,        // callback method for every time the element is updated
                onComplete: null       // callback method for when the element finishes updating
            };

            function formatter(value, settings) {
                return value.toFixed(settings.decimals);
            }
        }(jQuery));

        jQuery(function ($) {
            // custom formatting example
            $('.count-number').data('countToOptions', {
                formatter: function (value, options) {
                    return value.toFixed(options.decimals).replace(/\B(?=(?:\d{3})+(?!\d))/g, ',');
                }
            });

            // start all the timers
            $('.timer').each(count);

            function count(options) {
                var $this = $(this);
                options = $.extend({}, options || {}, $this.data('countToOptions') || {});
                $this.countTo(options);
            }
        });
    </script>
</body>
</html>
